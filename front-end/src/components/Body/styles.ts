import { createUseStyles } from "react-jss";

const styles = createUseStyles({
    body: {
        width: '100vw',
        minHeight: '80vh',
        position: 'absolute',
        top: '10vh',
        left: 0,
        right: 0,
        zIndex: 1,
    }
});

export default styles;
